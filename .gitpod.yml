image:
  file: .gitpod.Dockerfile

tasks:
  - name: Start Superset
    init: |
      docker compose pull
      docker compose build
    command: |
      docker compose up
ports:
  - port: 8088
    onOpen: open-preview
